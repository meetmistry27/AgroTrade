@model IEnumerable<AgroTrade.Models.Crop>

<h2>Crops</h2>

<table>
    <thead>
        <tr>
            <th>Crop Name</th>
            <th>Quantity</th>
            <th>Price</th>
            <th>Created At</th>
            <th>User ID</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var crop in Model)
        {
            <tr>
                <td>@crop.CropName</td>
                <td>@crop.Quantity</td>
                <td>@crop.Price</td>
                <td>@crop.CreatedAt</td>
                <td>@crop.UserId</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@crop.CropId">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@crop.CropId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Create">Create New Crop</a>
